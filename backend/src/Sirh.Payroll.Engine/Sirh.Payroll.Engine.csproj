<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <!-- Ce projet ne référence rien d'autre : le moteur de paie doit rester une fonction
       pure (mêmes entrées + même version de référentiel = toujours le même résultat). -->

</Project>
